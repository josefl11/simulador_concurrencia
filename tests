import threading
import time

documento = ""
lock = threading.Lock() #permite que solo un hilo se ejecute en ese momento

def usuario(nombre, texto):
    global documento
    for letra in texto:
        with lock: 
            documento += letra
            print(f"{nombre} escribió: {letra}")
        time.sleep(0.2)

personaA = threading.Thread(target=usuario, args=("Sebastian", "hais"))
personaB = threading.Thread(target=usuario, args=("Carla", "oag"))
personaC = threading.Thread(target=usuario, args=("Stiven", "lmo"))

personaA.start()
personaB.start()
personaC.start()

personaA.join()
personaB.join()
personaC.join()

print("\nDocumento final con Lock:", documento)

for letra in texto:
            documento += letra
            print(nombre)
